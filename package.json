{
  "name": "anthony-and-alicya-co-uk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "npx del-cli ./dist",
    "compile": "npx copyfiles ./src/**/*.* ./dist -u=1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "del-cli": "^3.0.1"
  }
}
